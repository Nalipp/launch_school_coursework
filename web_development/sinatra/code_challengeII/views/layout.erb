<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Users</title>

    <link rel="stylesheet" href="/stylesheets/users.css">
  </head>
  <body>
  <div class="wrapper">
  <%= yield %>
  <div class="push"></div>
  </div>
  <div class="footer">
    <% @users.each do |user| %>
      <a href="/<%= user.first %>" class="links"><%= user.first.capitalize if user.first.to_s != @name %></a>
    <% end %>
    <p>There are <%= @users.keys.count %> <a href="/">users</a> with a total of <%= count_interests %> interests.</p>
  </div>
  </body>
</html>
